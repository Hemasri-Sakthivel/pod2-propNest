package com.cog.propNest.module.identityAccessManagement.dto;

/**
 * Simple {@code { "message": "..." }} body returned by POST/PUT endpoints.
 */
public record MessageResponse(
        String message
) {
}
